
stacks: 
    ssl_proxy: docker-compose.ssl.proxy.yml

networks: 
    frontend-network: false
    backend-network: true

configs: 
    site.conf: site.conf

secrets: 
    site.key: site.key
    site.crt: site.crt

volumes:
    first_volume:
    second_volume:

env_files: 
    - environment.env