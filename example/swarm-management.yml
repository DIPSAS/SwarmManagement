
stacks: 
    ssl_proxy: docker-compose-files/docker-compose.ssl.proxy.yml

networks: 
    frontend-network: false
    backend-network: true

configs: 
    site.conf: DockerSSLProxy/site.conf

secrets: 
    site.key: DockerSSLProxy/site.key
    site.crt: DockerSSLProxy/site.crt

volumes:
    first_volume:
    second_volume:

env_files: 
    - environment.env